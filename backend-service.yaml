apiVersion: v1
kind: Service

metadata:
  name: netflix-backend-service
  namespace: default

spec:
  type: NodePort
  selector:
    app: netflix-backend
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30050